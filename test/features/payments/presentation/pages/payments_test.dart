// test/features/payments/presentation/pages/payments_test.dart

    // });
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ecommerce_pragma/features/payments/presentation/pages/payments.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

void main() {
  group('Payments Widget', () {
    // testWidgets('debe renderizar el widget principal', (
    //   WidgetTester tester,
    // ) async {
    // Arrange
    // await tester.pumpWidget(
    //   const ProviderScope(
    //     child: MaterialApp(
    //       home: Payments(total: '100', shipping: '10', subtotal: '90'),
    //     ),
    //   ),
    // );

    // // Act
    // await tester.pumpAndSettle();

    // // Assert
    // expect(find.byKey(const Key('payments_safe_area')), findsOneWidget);
    // expect(find.byKey(const Key('payments_material')), findsOneWidget);
    // expect(
    //   find.byKey(const Key('payments_checkout_template')),
    //   findsOneWidget,
    // );
  });
}
